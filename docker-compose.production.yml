volumes:
  pg_data:
  static:
  media:

services:
  db:
    image: postgres:13
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kittygram_user -d kittygram"]
      interval: 5s
      timeout: 5s
      retries: 5
    env_file: .env
    volumes:
      - pg_data:/var/lib/postgresql/data

  backend:
    image: glebusshek/kittygram_backend
    depends_on:
      db:
        condition: service_healthy
    env_file: .env
    volumes:
      - static:/backend_static
      - media:/app/media

  frontend:
    image: glebusshek/kittygram_frontend
    env_file: .env
    command: cp -r /app/build/. /frontend_static/
    volumes:
      - static:/frontend_static

  gateway:
    image: glebusshek/kittygram_gateway
    env_file: .env
    depends_on:
      - backend
    volumes:
      - static:/staticfiles
      - media:/mediafiles
    ports:
      - 9000:80
